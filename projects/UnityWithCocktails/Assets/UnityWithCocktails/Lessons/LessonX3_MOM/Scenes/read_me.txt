You can also, open and run any scene independently.

1. Open scene
2. Run Scene
3. (See in hierarchy that the SimpleGameManager is created automatically)

*Note: The buttons don't load by design. If needed, modify SimpleGameManager to add buttons dynamically.